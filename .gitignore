# Created by https://www.toptal.com/developers/gitignore/api/windows,visualstudiocode,react,yarn
# Edit at https://www.toptal.com/developers/gitignore?templates=windows,visualstudiocode,react,yarn

### react ###
.DS_*
*.log
logs
**/*.backup.*
**/*.back.*

node_modules
bower_components

*.sublime*

psd
thumb
sketch

### VisualStudioCode ###
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix

### VisualStudioCode Patch ###
# Ignore all local history of files
.history
.ionide

### Windows ###
# Windows thumbnail cache files
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Dump file
*.stackdump

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msix
*.msm
*.msp

# Windows shortcuts
*.lnk

### yarn ###
# https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored

.yarn/*
!.yarn/releases
!.yarn/patches
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# if you are NOT using Zero-installs, then:
# comment the following lines
!.yarn/cache

# and uncomment the following lines
# .pnp.*

# End of https://www.toptal.com/developers/gitignore/api/windows,visualstudiocode,react,yarn
# Created by https://www.toptal.com/developers/gitignore/api/windows,visualstudiocode,react,yarn # Edit at https://www.toptal.com/developers/gitignore?templates=windows,visualstudiocode,react,yarn ### react ### .DS_* *.log logs **/*.backup.* **/*.back.* node_modules bower_components *.sublime* psd thumb sketch ### VisualStudioCode ### .vscode/* !.vscode/settings.json !.vscode/tasks.json !.vscode/launch.json !.vscode/extensions.json !.vscode/*.code-snippets # Local History for Visual Studio Code .history/ # Built Visual Studio Code Extensions *.vsix ### VisualStudioCode Patch ### # Ignore all local history of files .history .ionide ### Windows ### # Windows thumbnail cache files Thumbs.db Thumbs.db:encryptable ehthumbs.db ehthumbs_vista.db # Dump file *.stackdump # Folder config file [Dd]esktop.ini # Recycle Bin used on file shares $RECYCLE.BIN/ # Windows Installer files *.cab *.msi *.msix *.msm *.msp # Windows shortcuts *.lnk ### yarn ### # https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored .yarn/* !.yarn/releases !.yarn/patches !.yarn/plugins !.yarn/sdks !.yarn/versions # if you are NOT using Zero-installs, then: # comment the following lines !.yarn/cache # and uncomment the following lines # .pnp.* # End of https://www.toptal.com/developers/gitignore/api/windows,visualstudiocode,react,yarn
영한


# https://www.toptal.com/developers/gitignore/api/windows,visualstudiocode,react,yarn에서 생성 # https://www.toptal.com/developers/gitignore?templates=windows,visualstudiocode,react,yarn에서 편집 ### 반응 ### .DS_* *.통나무 로그 **/*.지원.* **/*.뒤쪽에.* node_modules bower_components *.숭고한* psd 무지 스케치 ### VisualStudioCode ### .vscode/* !.vscode/settings.json !.vscode/tasks.json !.vscode/launch.json !.vscode/extensions.json !.vscode/*.code 스니펫 # Visual Studio Code의 로컬 히스토리 .역사/ # Visual Studio Code 확장 빌드 *.vsix ### VisualStudioCode 패치 ### # 파일의 모든 로컬 기록 무시 .역사 .이온화물 ### 윈도우 ### # Windows 썸네일 캐시 파일 Thumbs.db Thumbs.db:암호화 가능 ehthumbs.db ehthumbs_vista.db # 덤프 파일 *.stackdump # 폴더 구성 파일 [DD]esktop.ini # 파일 공유에 사용되는 휴지통 $RECLECLE.BIN/ # 윈도우 인스톨러 파일 *.택시 *.msi *.msix *.msm *.msp # 윈도우 단축키 *.lnk ### 실 ### # https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored .실/* !.원사/출시 !.원사/패치 !.원사/플러그인 !.yarn/sdks !.원사/버전 # 제로 설치를 사용하지 않는 경우: # 다음 줄을 주석 처리 !.원사/캐시 # 다음 줄의 주석 처리를 제거합니다. # .pnp.* # 끝 https://www.toptal.com/developers/gitignore/api/windows,visualstudiocode,react,yarn